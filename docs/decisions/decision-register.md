# Decision register

Per `R3CHAIN_GEOTHERMAL_PROTOTYPE_COMPLETION_SPEC.md` GOV-003. Tracks each named
domain-owner decision (Q4–Q9, Q11, Dr. Jan's PyDoublet semantic confirmations) plus
implementation-scoped decisions made during the prototype-completion work itself.
Source of truth for the *original* Q1–Q11 phase-0 questions remains
`docs/decisions/phase0-questions.md` — this register does not duplicate their full text,
only tracks status and rerun implications at a glance.

| ID | Owner | Status | Chosen value/policy | Source & date | Affected code/config | Rerun implications |
|---|---|---|---|---|---|---|
| Q1 | Jan | approved | No separate PyDoublet-MCP server; one R3-CHAIN server | Session-level, 2026-08-22; `docs/decisions/phase0-questions.md` Q1 | `src/r3chain_geothermal/mcp_server/*` | None — already reflected in shipped architecture |
| Q2 | Jan | provisional | Existing producer-wellhead temperature mapping accepted for the PoC | Session-level, 2026-08-22; ADR-002 | `parsers/pydoublet_parser.py` | Dr. Jan's domain-owner sign-off still outstanding |
| Q4 | Tanja | provisional | DH supply/return 70/40 °C | `config/demo_assumptions.json` | `adapter/`, `network/` | Changing requires a new config identity + sensitivity comparison (GOV-004) |
| Q5 | Tanja | provisional | Minimum HX approach 5 K | `config/demo_assumptions.json` | `adapter/heat_exchanger.py` | Same as Q4 |
| Q6 | Tanja | provisional | `cost_shortfall`/`auxiliary_supply`-style dispatch is the default; `strict_infeasible` now implemented as of this spec's Workstream E (not yet built as of this register's creation) | `docs/decisions/phase0-questions.md` Q6 | `network/candidate.py` | Policy choice itself still Tanja's; only the *implementation* of both modes is an engineering task, not a domain decision |
| Q7 | Tanja/partners | provisional | Placeholder CAPEX/OPEX values, no approved price year/source | `config/demo_assumptions.json`, Appendix A | `economics/costing.py` | Every economic output remains labelled illustrative until resolved |
| Q8 | Tanja | approved (session-level) | Feasibility-first, then lowest annualised cost | `docs/decisions/phase0-questions.md` Q8 | `economics/ranking.py` | Tie-break *order* still hard-coded, not yet config-driven (CFG-003) |
| Q9 | Tanja/architecture | approved | One-server integration; six-tool MCP boundary preserved | `docs/decisions/phase0-questions.md` Q9; MCP-001 | `mcp_server/*` | None |
| Q11 | Tanja/network owner | provisional | Minimum pressure 1.5 bar absolute | `docs/decisions/phase0-questions.md` Q11 | `network/pressure.py` | Numeric threshold still pending confirmation |

## Implementation-scoped decisions (this spec's own workstreams, not domain-owner questions)

| ID | Owner | Status | Chosen value/policy | Source & date | Affected code/config | Rerun implications |
|---|---|---|---|---|---|---|
| IMPL-001 | Claude (engineering choice, documented per GOV-004) | approved (superseded once mid-implementation, see below) | `expected_raw_sha256` is a **keyword-only parameter** of `parse_pydoublet_result()`/`run_workflow()`, **not** a field on `SourceProvenance` at all | `docs/issues/mcp-input-provenance-enforcement.md` IP-001 ("...or the validation request"); 2026-09-03 | `parsers/pydoublet_parser.py`, `workflow/core.py`, `mcp_server/schemas.py` (field stays only on the tool-facing `SourceProvenanceInput`), `mcp_server/tools.py` | **None whatsoever** — not just `run_id`, but `bundle_scientific_sha256` too, since `SourceProvenance` (embedded verbatim in the audit) never changes shape. **First attempt superseded**: adding the field directly to `SourceProvenance` (excluding it only from `compute_source_provenance_sha256`) was tried first, kept `run_id` stable, but still changed `bundle_scientific_sha256` for every caller (an explicit `null` in the embedded audit's `source_provenance` still changes the canonical JSON bytes `normalize_for_scientific_hash` hashes) — caught by the full test suite (`tests/mcp_client/test_wheel_install.py`, `tests/mcp_server/test_mcp_protocol.py` both failed, before/after diff: `90f52416...` → `7ac12adb...`, identical between both failing tests confirming CLI/MCP parity was never broken, only the golden literal was stale). Rejected per GOV-004 rather than approved as a rebaseline; the parameter-not-field redesign avoids the problem instead of requiring one. |
| IMPL-002 | Claude | approved | Provenance mismatch reuses the existing `ToolErrorCode.PYDOUBLET_VALIDATION_FAILED` MCP-level code (not a new 7th code) with `FailureCode.PYDOUBLET_RAW_HASH_MISMATCH` nested in `details.failure_code` | `docs/issues/mcp-input-provenance-enforcement.md` IP-003, 2026-09-03 | `mcp_server/errors.py` (unchanged — still exactly 6 codes), `errors.py` (new core `FailureCode` value) | None — `tests/mcp_server/test_errors.py`'s "exactly six codes" invariant stays true |
| IMPL-003 | Claude | approved | A hash mismatch on `geo_run_workflow` never creates a run directory (unlike every other stopping failure, which does get an audited "stopped" bundle) | `docs/issues/mcp-input-provenance-enforcement.md` IP-006, 2026-09-03 | `mcp_server/tools.py` (`_ProvenanceMismatchError`) | None — new code path only |

| IMPL-004 | Claude | approved | `summarize_workflow_result()` moved from `mcp_server/tools.py` to `mcp_server/schemas.py` | `mcp_server/registry.py`'s rehydration needs it too, 2026-09-03 | `mcp_server/schemas.py`, `mcp_server/tools.py` | None — `tools.summarize_workflow_result` remains a valid, identical reference (now imported rather than defined locally); `mcp_server/__init__.py`'s re-export and `mcp_client/cli_fallback.py`'s import are both unaffected; `tests/mcp_server/test_summarize_workflow_result.py`'s identity assertions still pass unmodified. Done to break a circular import (`registry.py` needing a function `tools.py` previously owned, while `tools.py` already imports from `registry.py`) |
| IMPL-005 | Claude | approved | `new_artifact_dir()`/`publish_artifact_dir()` use a deliberately weaker `_is_traversal_safe_path_component()` check, not the full `_RUN_ID_PATTERN` | Caught by the full test suite when first implemented with the strict pattern, 2026-09-03 | `mcp_server/registry.py` | ~18 pre-existing `tests/mcp_server/test_registry.py` concurrency tests use synthetic run_ids like `"x"` to exercise locking/eviction in isolation — not untrusted input, so the full pattern was needlessly restrictive there. The full `_RUN_ID_PATTERN` is still enforced at the one boundary that matters (`_rehydrate()`, reading directory names back from disk) |
| IMPL-006 | Claude | approved | Persistent registry is opt-in via `R3CHAIN_RUN_ROOT`, not the new unconditional default | `docs/issues/mcp-persistent-run-registry.md` RR-001 ("...MAY override the default"), 2026-09-03 | `mcp_server/config.py`, `mcp_server/server.py` | None — every existing test calling `build_server()`/`RunRegistry()` without the env var set is byte-for-byte unaffected; an unconditional default would have made tests read/write a real `~/.local/share/...`-style path and leak state between test runs |
| IMPL-007 | Claude | approved | CFG-003/DSP-001-004 (`R3CHAIN_GEOTHERMAL_PROTOTYPE_COMPLETION_SPEC.md` Workstreams D/E): wired `gates.max_pump_dp_bar` and `gates.heat_delivery_tolerance_fraction` — both previously declared in `config/demo_assumptions.json` but never consumed by any gate — into real checks (`PUMP_DIFFERENTIAL_PRESSURE_EXCEEDED` on both the main plant pump and the geothermal injection pump; `GEOTHERMAL_HEAT_SHORTFALL` under a new `strict_infeasible` policy path). Kept the project's existing `"cost_shortfall"`/`"strict_infeasible"` config vocabulary rather than renaming to the spec's own `"auxiliary_supply"`/`"strict_infeasible"` naming (same policy meaning; config/demo_assumptions.json's own `auxiliary_policy_options` already lists both, so no config-file edit was needed) — CFG-004's gate order (pressure → pump-dp → velocity → mass-balance → energy-balance) was also applied by reordering the existing checks in both `network/baseline.py` and `network/candidate.py` | `network/errors.py`, `network/baseline.py`, `network/candidate.py`; 2026-09-03 | `network/baseline.py` (`GateTolerances.max_pump_dp_bar`, gate reorder), `network/candidate.py` (`GeothermalInjectionPolicy.heat_delivery_tolerance_fraction`, strict-infeasible support, gate reorder), `network/errors.py` (new `PUMP_DIFFERENTIAL_PRESSURE_EXCEEDED`/`GEOTHERMAL_HEAT_SHORTFALL` codes) | `BASELINE_CONTRACT_SCHEMA_VERSION`/`CANDIDATE_CONTRACT_SCHEMA_VERSION` bumped 1.0.0 → 1.1.0 (NFR-007: new fields on an embedded, audited model). This changes `bundle_scientific_sha256` for every bundle (golden literal moved `90f52416785f0ea8f7f8dc33ede68c9b5529e6e9d51dd60e8d2e1df0389b8d2f` → `fd1e3408cbccfeb81d2847a60d809c2c8e407fb26de4738a624cb28ad00456f6`, updated in `tests/mcp_client/test_wheel_install.py`, `tests/mcp_server/test_mcp_protocol.py`, `tests/mcp_server/test_input_provenance_mcp.py`) but does **not** change `run_id` (`r3chain-run-93d41133daa11d1a`, unaffected — it never depends on `GateTolerances`/`GeothermalInjectionPolicy`) and does **not** change any canonical C1-C4 numeric value (feasibility, KPIs, or the LCOH-based ranking: `{"C1": 52.1714, "C2": 52.2602, "C3": 52.3489, "C4": 52.4821}` unchanged, reasserted by the same tests right after the rebaselined hash) — both are re-verified live by the existing tests, not merely asserted. GOV-004 process followed: stopped on the full-suite hash-mismatch, diffed exactly (only `bundle_scientific_sha256`, nothing else), identified the precise cause (the schema bump), classified it as an intended, CFG-003-mandated fix, and rebaselined only the one literal this required — not a broader or unexplained change. Full suite: 885 passed (was 875 before this phase), 0 failed |

| IMPL-008 | Claude | approved | DSP-005/DSP-006: implemented a bounded BISECTION root solve for the geothermal injection branch's self-consistent mass-flow sizing, not the literally-worded damped fixed-point iteration ("apply relaxation if required") — the fixed-point form was implemented and tested first, and diverges at every relaxation factor tried (1.0 down to 0.05), because the map from injection mass flow to solved return temperature is extremely steep (near-vertical) right at the design-temperature guess, which itself sits at the edge of the hydraulically feasible range for this topology/margin. DSP-005 point 5 explicitly permits "a bounded deterministic iteration **or root solve**"; the achieved-outlet-temperature deviation is empirically monotonic across the feasible range, so bisection (bracket-search then binary search) was substituted — a documented engineering decision (GOV-004/Section 23), not a silent substitution. The canonical default policy/margin are left unchanged per DSP-006's own instruction not to silently select a new physical policy from numerical convenience — the sensitivity findings are reported, not acted on | `docs/issues/self-consistent-flow-solver.md`, 2026-09-03 | `network/candidate.py` (`GeothermalInjectionPolicy.injection_sizing_policy`, `_solve_self_consistent_injection`, `SelfConsistentFlowDiagnostics`, `CandidateFailureCode.SELF_CONSISTENT_FLOW_NOT_CONVERGED`), `network/__init__.py` (new public exports) | `CANDIDATE_CONTRACT_SCHEMA_VERSION` bumped 1.1.0 → 1.2.0 (two new fields on an embedded, audited model). Moves `bundle_scientific_sha256` again for the canonical golden run (`fd1e3408cbccfeb81d2847a60d809c2c8e407fb26de4738a624cb28ad00456f6` → `f85243d16a6e43365f12081a6af346d2ea1aa5bbb51a70f760404eb64a1188a1`, updated in the same three pinned tests as IMPL-007) but does **not** change `run_id` (unaffected both because it never depends on this model AND because `config/demo_assumptions.json`'s raw bytes were not touched — the new `injection_sizing_policy` field is read via `.get()`, defaulting to `"fixed_design_temperature"`, its absence from the canonical config byte-for-byte reproducing prior behavior) and does **not** change any canonical C1-C4 KPI/feasibility/LCOH-ranking value — re-verified live by the same tests. Full suite: 903 collected, 902 passed, 1 failed (a pre-existing subprocess-timing test, confirmed unrelated and flaky under load, not caused by this change) |

| IMPL-009 | Claude | approved | FAIL-001..004: added the intentionally infeasible workshop/negative-demo candidate (`C5_negative`) via a genuinely SEPARATE config file (`config/demo_assumptions_workshop_negative.json`), not by editing the canonical `config/demo_assumptions.json` (FAIL-001's explicit instruction). Discovered candidates are NOT config-driven at runtime at all -- `build_default_blueprint()` builds them from `network/geometry.py`'s Python constants, so `config/demo_assumptions.json::candidates.list` is descriptive only (a CFG-001 gap noted, not closed, by this issue) -- added a real code path (`workflow/core.py::_apply_workshop_negative_demo()`) instead of an unenforced JSON-only entry | `docs/issues/workshop-negative-demo.md`, 2026-09-03 | `workflow/core.py` (`_apply_workshop_negative_demo`, wired into `run_workflow()` Stage 3 and `validate_config_structure()`), `workflow/svg_export.py` (candidate-marker fallback for a candidate outside `CANDIDATE_SITE_COORDINATES`), `config/demo_assumptions_workshop_negative.json` (new file) | None for the canonical config -- `_apply_workshop_negative_demo()` reads both new keys via `.get(...)`, absent from `config/demo_assumptions.json` on purpose, so it is a pure no-op there (not even a copy): `config_sha256`, `run_id` (`r3chain-run-93d41133daa11d1a`), `bundle_scientific_sha256`, and every C1-C4 KPI/feasibility/LCOH-ranking value are unaffected, re-verified live by a dedicated test. No schema version bump needed (no new Pydantic fields; `BlueprintCandidate`/`NetworkBlueprint` are reused unchanged). Running the separate workshop config naturally produces a different `run_id` (`r3chain-run-440e06b197eaae18`) -- expected for a deliberately different scenario. Full suite: 918 passed (was 903), 0 failed |

| IMPL-010 | Claude | approved | DLT-001..007 (Workstream G): implemented the reusable geothermal-doublet-connection composite (`network/doublet_component.py`) as a typed WRAPPER around `network/candidate.py`'s own already-tested private functions (`_add_geothermal_injection_branch`, `_solve_self_consistent_injection`, `_compute_injected_mass_flow_kg_s`), not a re-derivation -- guarantees parity by construction rather than by a tolerance check. Per the Phase-4 exit gate's own two-clause reading ("component parity and isolation pass; legacy duplicate construction logic is removed only after parity is proven"), `evaluate_candidate()` itself is NOT retargeted to call the new component in this phase -- a deliberately conservative choice given how heavily validated that function already is, and because there is no actual duplicate physics to remove today (both paths already share the same calls) | `docs/issues/geothermal-doublet-component.md`, `docs/geothermal-doublet-component-guide.md`, 2026-09-03 | `network/doublet_component.py` (new file), `network/__init__.py` (new public exports) | None -- purely additive, new module/exports only; no existing file's behavior changed. Parity verified for all four candidates under both `injection_sizing_policy` values, bit-identical (`rel_tol=1e-12`) between the new component and `evaluate_candidate()`'s own results. Full suite: 938 passed (was 918), 0 failed |

| IMPL-011 | Claude | approved | CAN-001..007 (Workstream H): implemented deterministic candidate generation (`network/candidate_generation.py`) as an entirely additive module -- 8 trunk-attachment candidates (reusing C1-C4's own exact approved distances) plus 8 new consumer-attachment candidates, screened via 7 typed reasons. Two of the seven screening reasons (`ROUTE_LENGTH_EXCEEDS_LIMIT`, `EXCLUDED_PROTECTED_GEOMETRY`) occur naturally on the canonical demonstration; three more are reachable through the real `generate_candidates()` call with contrived arguments (a broken blueprint, a non-positive DN, a monkeypatched route table); the remaining two (`INVALID_PRESSURE_ZONE_PAIRING`, `COMPONENT_CONSTRUCTION_CONFLICT`) are genuinely unreachable by this generator's own loop structure and this synthetic network's single pressure zone, proven only at the type level -- documented explicitly rather than silently left untested. A real, unplanned finding: 3 of the 11 accepted candidates (the non-excluded consumer attachments) fail `VELOCITY_LIMIT_EXCEEDED` when actually evaluated through the unmodified `evaluate_candidate()` (their DN100 branch pipes can't also carry injection flow) -- kept and documented as a genuine technical result, not adjusted away, since AC-08 only requires independent constructability/evaluability, not technical feasibility | `docs/issues/candidate-generation.md`, 2026-09-03 | `network/candidate_generation.py` (new file), `network/__init__.py` (new public exports) | None -- purely additive; `run_workflow()`'s existing predefined-candidate orchestration and `config/demo_assumptions.json` are both untouched. The one generated candidate that shares a real predefined candidate's exact junction pair and length (`trunk_1-direct` vs. `C1`) was verified to produce numerically identical evaluation results. Full suite: 962 passed (was 938), 0 failed |

| IMPL-012 | Claude | approved | DATA-001..009 (Workstream I): implemented `src/r3chain_geothermal/data_contracts/` -- a new, independent top-level package of typed study-package contracts, a pre-flight semantic validator, a readiness reporter, and DATA-008's synthetic sample package plus an intentionally incomplete real-mode fixture. GIS layers are represented at the metadata level (CRS/hash/feature-count declarations) rather than by parsing GeoJSON geometry -- no new GIS library dependency was introduced (NFR-005). A design correction made and caught during implementation: the first network-topology-connectivity check required the WHOLE junction set (supply+return) to form one connected pipe graph, which immediately misclassified the synthetic sample package itself as disconnected, since a real two-sided DH network's supply and return graphs are legitimately separate (joined only through non-pipe components this schema doesn't model); corrected to check connectivity per side | `docs/issues/real-data-contracts.md`, 2026-09-03 | `data_contracts/schema.py`, `data_contracts/validation.py`, `data_contracts/readiness.py`, `data_contracts/sample_packages.py`, `data_contracts/__init__.py` (all new files) | None -- purely additive, a new independent package with no existing file changed. AC-09 verified directly: the incomplete package (missing CRS, a pipe diameter, and PyDoublet provenance) fails pre-flight with exactly those three typed errors, never raises, and both real-mode optimisation permission flags are denied; the synthetic sample package validates cleanly and both permission flags are granted. Full suite: 994 passed (was 962), 1 failed (the same pre-existing, confirmed-unrelated, load-flaky subprocess-timing test) |

| IMPL-013 | Claude | approved | OPT-001..007 (Workstream J, the final phase before Phase 8's external-data gate and Phase 9's release acceptance): implemented the synthetic joint site/connection optimisation demonstration as an outer layer reusing existing physics/economics functions unchanged (`adapter.evaluate_heat_exchanger_coupling`, `network.evaluate_candidate`, `economics.compute_candidate_economics`) -- no new formulas. Three synthetic geothermal scenarios are DERIVED from the one golden PyDoublet fixture (producer temperature/mass flow adjusted, raw power RECOMPUTED via the same energy-consistency formula T1.5B itself uses, so each variant passes that unrelated check by construction and isolates the intended failure mode). A CURATED six-alternative demonstration (not a full scenario x candidate cross-product, judged disproportionate) satisfies every OPT-005 minimum, measured directly rather than merely asserted -- including an unplanned finding kept and reported: the reduced-brine-flow scenario's alternatives are Pareto-dominated by the golden scenario's own, on every considered objective. No approved multi-objective weighting policy exists, so a Pareto/non-dominated shortlist is returned (OPT-003), never invented weights. OPT-006's real-mode readiness check is documented as an explicit, not-yet-wired deferral alongside Phase 8 itself | `docs/issues/joint-location-optimization.md`, 2026-09-03 | `workflow/joint_optimization.py`, `workflow/joint_optimization_export.py`, `workflow/__init__.py` (new exports) (all new files except `__init__.py`) | None -- purely additive; no existing file's behavior changes. AC-10 verified directly: technical failures never carry economics; the demonstration returns a Pareto shortlist, not an invented ranking; the exported recommendation explicitly states the result is synthetic. Full suite: 1009 passed, 0 failed (a fully clean run) |

| IMPL-014 | Claude | approved | Phase 9 (release acceptance): (1) MCP-002 hardening -- `geo_get_capabilities` now additionally advertises provenance-hash-enforcement support, live persistent-registry status, available shortfall policies, available injection-sizing policies, and candidate-generation modes (all capabilities added across Phases 1-7 that were not yet surfaced in the capabilities response); (2) a genuinely fresh, isolated Python 3.11 venv built the wheel, installed it with the `mcp` extra, and ran BOTH the canonical config (via the live stdio MCP client) and the workshop-negative config (via the deterministic CLI) from directories outside the repository, confirming the exact golden `run_id`/`bundle_scientific_sha256`/LCOH set is unchanged after all seven phases and that the negative-demo config produces a correctly different, expected run identity; (3) `docs/traceability-matrix.md` -- a row for every requirement ID in the completion spec, with PARTIAL/BLOCKED items reported honestly rather than collapsed into PASS | `docs/traceability-matrix.md`, 2026-09-03 | `mcp_server/schemas.py` (`CapabilitiesSummary` new fields), `mcp_server/tools.py` (`get_capabilities()`), `tests/mcp_server/test_tools.py` (new test) | None -- the capabilities addition is a new-fields-only, non-schema-breaking change to a live tool RESPONSE (never persisted into any run's audit bundle, so it carries no `run_id`/`bundle_scientific_sha256` impact at all, unlike every earlier schema-version bump this project made). Full suite: 1010 collected, 1009 passed, 1 failed (the same pre-existing, confirmed-unrelated, load-flaky subprocess-timing test, re-confirmed passing in isolation) |

| IMPL-015 | Claude | approved | R3CHAIN_GEOTHERMAL_PROTOTYPE_COMPLETION_SPEC.md Phase 2 (release blocker, second-pass audit): diagnosed the reported macOS-vs-Linux `bundle_scientific_sha256` divergence (`f85243d1...` vs. an independently-reported `45b7040d...`) despite matching `run_id`/feasibility/ranking/KPI values -- root cause: `normalize_for_scientific_hash()` previously quantized nothing numeric, only removing `created_at`, so any platform-level BLAS/LAPACK floating-point noise (typically below the 13th-15th significant digit for pandapipes' sparse linear solves) propagated straight into the hash via JSON's exact-repr float formatting. Fixed by quantizing every float to 12 significant figures (`SCIENTIFIC_HASH_FLOAT_SIGNIFICANT_FIGURES`) before hashing -- finer than every gate tolerance in this project (loosest 2%) so a genuinely different result can never be masked, coarser than typical cross-platform noise so the SAME physical solution now hashes identically. Explicitly documented as NOT an empirically-verified fix for the exact reported Linux hash (no Linux machine was available to re-test against) -- a principled, tested mechanism for the diagnosed class of noise, with its own honesty-boundary statement in code | `hashing.py`'s own new docstrings, 2026-09-03 | `hashing.py` (`SCIENTIFIC_HASH_FLOAT_SIGNIFICANT_FIGURES`, `_quantize_float_for_scientific_hash`, `normalize_for_scientific_hash`), `tests/test_hashing.py` (5 new tests) | `SCIENTIFIC_NORMALIZATION_RULE_VERSION` bumped 1.0.0 -> 1.1.0. Moves `bundle_scientific_sha256` for the canonical golden run again (`f85243d16a6e43365f12081a6af346d2ea1aa5bbb51a70f760404eb64a1188a1` -> `d67cb2f32de228ee1a6b0ac8f4e9c7e05eb55ba2d49d6578ea79692f1a359f46`, updated in the same three pinned tests as every prior rebaseline) but does **not** change `run_id` and does **not** change any canonical C1-C4 KPI/feasibility/LCOH-ranking value -- re-verified live by the same tests. All 11 pre-existing `tests/test_hashing.py` tests pass completely unchanged (the chosen 12-significant-figure budget was verified against the existing test payloads' own exact values, e.g. `4345.417312`, before being finalized, specifically so no existing test needed a value change, only new tests were added) |

| IMPL-016 | Claude | approved | R3CHAIN_GEOTHERMAL_PROTOTYPE_COMPLETION_SPEC.md Phase 3.1: retargeted `network/candidate.py::evaluate_candidate()` to call `network/doublet_component.py::build_and_evaluate_geothermal_doublet_with_net()` instead of directly sequencing `_add_geothermal_injection_branch`/`_solve_self_consistent_injection` itself -- reversing IMPL-010's deliberate deferral, per this specification's explicit instruction not to preserve duplicate implementations without a documented reason. The underlying physics functions were already shared (IMPL-010), so this closes the remaining ORCHESTRATION-level duplication (typed-input construction, the fixed/self-consistent dispatch, result extraction), making the doublet component the single authoritative call path for both the primary workflow and standalone/library callers. `evaluate_candidate()` now builds the four DLT-001 typed inputs from its own `coupling_result`/`candidate`/`injected_kw`/`injection_policy`, delegates once, maps a `GeothermalDoubletFailure` directly onto `CandidateEvaluationFailure` (shared `CandidateFailureCode`), and reads `district_heating_water_mass_flow_kg_s`/pressures/temperatures/`flow_solver` off the returned `GeothermalDoubletResult` rather than re-extracting them from `net` a second time; it still applies its own whole-network gates (consumer temperature, absolute pressure, pump differential pressure, pipe velocity, mass/energy balance) directly against the returned `net`, exactly as before, since those remain outside the component's documented scope. One genuine cross-module bug was found and fixed during this work, not merely a test accommodation: `DoubletOperatingPolicy`'s three solver-tolerance field defaults were copied from `network.candidate`'s constants at `doublet_component.py`'s own import time (`from .candidate import NAME`), so a test that monkeypatches `network.candidate.SELF_CONSISTENT_FLOW_MAX_ITERATIONS` directly (`test_self_consistent_flow_not_converged_via_monkeypatched_max_iterations`) produced a mismatched `details["max_iterations"]` in the returned failure, since `_solve_self_consistent_injection()` (defined in, and reading its global from, `network.candidate` itself) correctly saw the patched value while the doublet component's copied-at-import-time default did not. Fixed by referencing these three constants through a module reference (`from . import candidate as _candidate_module`) with `Field(default_factory=...)` in place of a plain default, in both the field defaults and the validator's equality check, so both always re-read the live module attribute. The value used in every non-monkeypatched execution (40 iterations) is unchanged | `docs/issues/geothermal-doublet-component.md` (updated in place, prior "not retargeted" section superseded), 2026-09-03 | `network/candidate.py` (`evaluate_candidate()` body), `network/doublet_component.py` (`DoubletOperatingPolicy` field defaults/validator, `build_and_evaluate_geothermal_doublet_with_net()` now additionally returns the solved net, `build_and_evaluate_geothermal_doublet()` becomes a thin wrapper), `network/__init__.py` (new `build_and_evaluate_geothermal_doublet_with_net` export) | None -- purely a call-path/internal-plumbing change; `CandidateEvaluationResult`'s own field set, values, and JSON contract are completely unchanged (proven by the pre-existing exact-value-pinned tests in `tests/network/test_candidate.py` passing with zero test-file edits beyond the one genuine bug fix above). Does not change `run_id`, `bundle_scientific_sha256`, or any canonical C1-C4 KPI/feasibility/LCOH-ranking value. `tests/network/test_candidate.py` + `tests/network/test_doublet_component.py` + `tests/network/test_baseline.py`: 159 passed, 0 failed |

| IMPL-017 | Claude | approved | R3CHAIN_GEOTHERMAL_PROTOTYPE_COMPLETION_SPEC.md Phase 3.2: wired `network/candidate_generation.py::generate_candidates()` (CAN-001..007, previously additive-library-only per IMPL-011) into `workflow/core.py::run_workflow()`/`validate_config_structure()` via a new `_apply_candidate_mode()` function, reading `config["candidates"]["mode"]` (default `"predefined"`, absent from the canonical config so its raw bytes/config_sha256/run_id are completely unaffected -- same established opt-in pattern as FAIL-001/DSP-005). `"generated"` REPLACES `blueprint.candidates` with `generate_candidates()`'s own ACCEPTED output (via `GeneratedCandidateSpec.to_blueprint_candidate()`), evaluated through the identical, unmodified `evaluate_candidate()` pathway -- reachable through `geo_run_workflow` by pointing the one-server MCP architecture's fixed config at a config file with `candidates.mode=="generated"` (`R3CHAIN_MCP_CONFIG_PATH`), the same mechanism the workshop-negative-demo config already uses; no seventh MCP tool, no new tool parameter. Adds `candidates.generated.max_candidates` (optional, deterministic cap on the already-sorted accepted list) and a loud `ValueError`/`WorkflowConfigurationError` when generation accepts zero candidates. Verified end-to-end through the full CLI/artifact pipeline (`config/demo_assumptions_generated_candidates.json`: 16 screened, 11 accepted, 8 feasible/ranked, 3 `VELOCITY_LIMIT_EXCEEDED`, deterministic run_id `r3chain-run-e1dd8da8e6c6a91e` distinct from the canonical golden run, all seven artifact files produced, SVG parses as valid XML). Also fixed a genuine (if minor) honesty-boundary issue surfaced by this work: `economics/ranking.py::SHARED_CAPEX_STATEMENT` (rendered verbatim into every `recommendation.md`) previously hardcoded the literal text "identical across C1-C4", which would misleadingly describe a generated-mode run's own, differently-named candidate set; reworded to "identical across every candidate evaluated in this run (predefined or generated)", a value-identity-only test (`ranking.shared_capex_statement == SHARED_CAPEX_STATEMENT`) unaffected. `mcp_server/schemas.py::CapabilitiesSummary.candidate_generation_modes`'s own docstring updated to remove the now-stale "not yet wired into geo_run_workflow" caveat | `docs/issues/candidate-generation.md` (updated in place, "not covered" section superseded), 2026-09-03 | `workflow/core.py` (`_apply_candidate_mode()`, both call sites), `economics/ranking.py` (`SHARED_CAPEX_STATEMENT` wording), `mcp_server/schemas.py` (docstring only), `config/demo_assumptions_generated_candidates.json` (new file), `tests/workflow/test_generated_candidates.py` (new file, 14 tests) | The `_apply_candidate_mode()` feature itself is purely additive: the canonical predefined-mode config's `config_sha256`/`run_id`/every KPI/feasibility/ranking value are unaffected (verified: canonical run still returns `r3chain-run-93d41133daa11d1a` with C1-C4 numerically unchanged). Does not touch `WORKFLOW_CONTRACT_SCHEMA_VERSION` (candidate-SET composition was already outside the versioned contract's own concern, per FAIL-001's precedent). HOWEVER the accompanying `SHARED_CAPEX_STATEMENT` wording fix (above) DOES move `bundle_scientific_sha256` for the canonical golden run a third time this session, since that string is rendered into `RankingResult.shared_capex_statement`, part of every bundle including the canonical one -- causally isolated via `git stash` on `ranking.py` alone (test passed with the old wording, failed with the new one, confirming sole cause) before rebaselining, per GOV-004: `d67cb2f32de228ee1a6b0ac8f4e9c7e05eb55ba2d49d6578ea79692f1a359f46` -> `ee76b2a626f57fd4825c554ac55e57e81e567f86c7bf4acd771cb23a4389f3c8`, updated in the same three pinned tests as every prior rebaseline (IMPL-011's own predecessor entries). `tests/workflow/` + `tests/mcp_server/` + `tests/network/test_candidate_generation.py`: 371 passed, 0 failed; the full suite's own 4 pinned-hash tests failed until this rebaseline, then passed -- see the full-suite re-run recorded immediately below this entry |

| IMPL-018 | Claude | approved | R3CHAIN_GEOTHERMAL_PROTOTYPE_COMPLETION_SPEC.md Phase 4: implemented `run_joint_optimization_full_product()` (`workflow/joint_optimization.py`) -- the FULL deterministic product of every synthetic geothermal scenario x every accepted `generate_candidates()` output (3 x 11 = 33 alternatives, measured, no curation, `len(result.alternatives)` equals the unfiltered search-space size by construction), superseding the prior curated six-alternative demonstration's own exclusivity (that function, `run_joint_optimization_demo()`, is UNCHANGED and still available). Wrapped in a NEW top-level workflow entry point (`workflow/joint_workflow.py`, new file) -- `run_joint_optimization_workflow()`/`JointOptimizationWorkflowResult`/`JointOptimizationWorkflowFailure`, its own `JointOptimizationManifestRecord`-audited artifact bundle (`write_joint_optimization_artifacts()`) -- rather than a branch inside `run_workflow()` itself, since `WorkflowResult`'s own shape (a flat candidate set, a single lowest-cost ranking) cannot honestly represent a `(scenario, candidate)` grid with a Pareto shortlist (OPT-003: no approved multi-objective weighting policy exists) without either inventing a rank or reusing `ManifestRecord`'s own single-scenario-specific filename invariant incorrectly. Reachable from the CLI (`r3chain-geothermal-demo`) via the SAME config-driven mode-switch convention Phase 3.2 established for `candidates.mode` -- `config["joint_optimization"]["enabled"]`, absent from the canonical config so it is completely unaffected. `geo_run_workflow`'s own MCP dispatch is explicitly DEFERRED (not silently incomplete): `RunSummary.ranked` requires an integer `rank` per entry, which a Pareto shortlist has no honest way to supply; extending the MCP response union with a genuinely new discriminated success variant is a real API-contract decision needing its own review, in the same spirit as OPT-006's own documented real-mode-safeguard deferral. `geo_get_capabilities` makes no claim about joint-optimization reachability through `geo_run_workflow` (verified: `mcp_server/tools.py`/`server.py` contain no reference to it at all), so no capability is falsely advertised by this work | `docs/issues/joint-optimization-workflow-integration.md` (new file), `docs/issues/joint-location-optimization.md` (updated in place), 2026-09-03 | `workflow/joint_optimization.py` (`run_joint_optimization_full_product()`, additive), `workflow/joint_workflow.py` (new file), `workflow/cli.py` (dispatch to the joint-optimization path when enabled, `_run_joint_optimization_cli()`), `workflow/__init__.py` (new exports), `tests/workflow/test_joint_workflow.py` (new file, 15 tests) | None -- purely additive; `run_workflow()`, `WorkflowResult`, `write_workflow_artifacts()`, and every existing MCP tool are byte-for-byte unmodified. Does not touch `WORKFLOW_CONTRACT_SCHEMA_VERSION`, `run_id` computation, or any canonical C1-C4/generated-candidates config's own hash or ranking. `tests/workflow/test_joint_optimization.py` + `tests/workflow/test_joint_workflow.py` + `tests/workflow/test_cli.py`: all passing; full suite (1044 tests) passes, 0 failed |

Dr. Jan's outstanding PyDoublet semantic confirmations (Q2/Q3 in `phase0-questions.md`) remain
unresolved by this work and are not claimed to be resolved by it.
