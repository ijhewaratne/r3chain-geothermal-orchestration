# T5.1C — Claude Desktop acceptance replay (Phase 9)

This must genuinely occur in Claude Desktop, not Claude Code — it is the one item this whole body
of work cannot complete on its own. Everything below is exact: file paths, prompt text, and the
expected numeric results this session independently reproduced through a clean-clone → fresh venv
→ wheel build → separate fresh venv → wheel install → run pipeline (`docs/decisions/decision-
register.md`, this document's own "Independent verification already performed" section).

## 1. One-time setup

Add this to your `claude_desktop_config.json` (adjust `R3CHAIN_RUN_ROOT` to a real, stable directory
on your machine — this is what makes the run registry PERSISTENT across a server restart, the
specific thing Session 2 below tests):

```json
{
  "mcpServers": {
    "r3chain-geothermal": {
      "command": "r3chain-geothermal-mcp-server",
      "args": [],
      "env": { "R3CHAIN_RUN_ROOT": "/Users/<you>/r3chain-mcp-runs" }
    }
  }
}
```

Fully quit and relaunch Claude Desktop so it loads this config and starts the MCP server fresh.

## 2. Files to attach (Session 1)

Attach this exact file to your first message:

- `fixtures/pydoublet/repaired_result.json`

You do not need to attach the provenance file — its four fields are short enough to be typed
directly in the prompt below.

## 3. Exact prompt to paste (Session 1)

```
You are connected to the r3chain-geothermal MCP server, which exposes six tools:
geo_get_capabilities, geo_validate_pydoublet_result, geo_run_workflow, geo_get_run_summary,
geo_get_audit, geo_get_artifact.

Call them in exactly this order:

1. geo_get_capabilities
2. geo_validate_pydoublet_result, passing the attached raw PyDoublet result and this source
   provenance:
   - source_pydoublet_commit: 0d649c3e6930d342dac03654d57776e134c2d0b9
   - source_format_hint: known_repaired
   - calculation_mode: deterministic
   - expected_raw_sha256: 6c42d3368883070cd177ecb02572480d3aab4238e4781357b78c742cec642762
3. geo_run_workflow, with the SAME raw result and SAME provenance (including expected_raw_sha256)
   as step 2
4. geo_get_run_summary, using the run_id that geo_run_workflow returned
5. geo_get_audit, using the same run_id
6. geo_get_artifact on "workflow_result.json", using that same run_id — call it repeatedly,
   increasing offset by the previous call's own returned length each time, until a response comes
   back with next_offset equal to null. Tell me how many geo_get_artifact calls that took.

Hard rules:
- Never calculate, estimate, or approximate any physics, economics, feasibility, or ranking result
  yourself — every number must come from a tool's own response.
- Preserve and report every warning verbatim, never summarized away.
- If any tool returns a status of "error", report its exact code, message, and stage — do not
  paraphrase it.

When done, report in this exact order: (a) the full sequence of tool calls you made, in order;
(b) the run_id and bundle_scientific_sha256 geo_run_workflow/geo_get_run_summary returned; (c) the
number of geo_get_artifact calls needed to reach next_offset: null; (d) every warning any tool
returned, verbatim, or "none" if there were none; (e) the preferred candidate and why, citing only
the tool's own ranking.
```

## 4. Expected results (Session 1)

| Item | Expected value |
|---|---|
| Tool call order | `geo_get_capabilities` → `geo_validate_pydoublet_result` → `geo_run_workflow` → `geo_get_run_summary` → `geo_get_audit` → `geo_get_artifact` (one or more times) |
| `run_id` | `r3chain-run-93d41133daa11d1a` |
| `bundle_scientific_sha256` | `ee76b2a626f57fd4825c554ac55e57e81e567f86c7bf4acd771cb23a4389f3c8` |
| `geo_validate_pydoublet_result`'s own `raw_result_sha256` | `6c42d3368883070cd177ecb02572480d3aab4238e4781357b78c742cec642762` (must match the `expected_raw_sha256` you supplied — this IS the strict-hash-enforcement check; if it did not match, the call would instead return a `PYDOUBLET_RAW_HASH_MISMATCH` error) |
| Preferred candidate | `C1`, rank 1 of 4 feasible |
| Full ranked LCOH set (EUR/MWh) | C1 ≈ 52.1714, C2 ≈ 52.2602, C3 ≈ 52.3489, C4 ≈ 52.4821 |
| Warnings | none expected for this canonical input/config pair |
| `geo_get_artifact` calls to reach `next_offset: null` on `workflow_result.json` | more than one (the file is ~282 KB; the default page size is well under that) — the exact count depends on the page size Claude's own tool-calling loop chooses, so no single number is "correct," only that it eventually reaches `null` |

## 5. Session 2 — restart recovery (the actual acceptance step for RR-005/RR-008)

After Session 1 completes successfully: **fully quit Claude Desktop** (not just close the window —
this must actually terminate the MCP server subprocess), then relaunch it and start a **new chat**.

Paste:

```
Using the r3chain-geothermal MCP server, call geo_get_run_summary and then geo_get_audit for
run_id r3chain-run-93d41133daa11d1a. Then call geo_run_workflow again with the exact same raw
PyDoublet result and provenance as before (I can re-attach the file if you need it). Report:
the bundle_scientific_sha256 from geo_get_run_summary, and whether geo_run_workflow's own response
indicates this run was reused rather than recomputed.
```

**Expected**: `geo_get_run_summary`/`geo_get_audit` succeed immediately (this run_id was rehydrated
from `R3CHAIN_RUN_ROOT` at the new server process's own startup — you never re-ran the workflow to
get this response), `bundle_scientific_sha256` is IDENTICAL to Session 1's value above, and
`geo_run_workflow`'s response indicates the run was reused (`reused_existing_run: true`), not
recomputed.

## 6. Evidence to return

Paste back, or describe:

1. The full tool-call sequence and order Claude Desktop actually made in both sessions.
2. The exact `run_id` and `bundle_scientific_sha256` Claude reported in Session 1.
3. Confirmation `geo_get_artifact` pagination reached `next_offset: null`, and roughly how many
   calls it took.
4. Every warning Claude reported (or "none").
5. Confirmation Session 2 (after a genuine restart) returned the SAME `run_id`/
   `bundle_scientific_sha256` and reported the run as reused, not recomputed.
6. Any discrepancy at all from the expected values above — including the exact wording Claude used,
   even if it seems minor. A discrepancy here is exactly the kind of thing this whole project's own
   GOV-004 process exists to catch, not something to round off.

## Independent verification already performed (2026-09-03, this session, NOT a substitute for the above)

A clean `git clone` into a new directory, a fresh `python3.11 -m venv`, `pip install -e ".[dev,mcp]"`,
and the full offline test suite (1060 tests) all succeeded from scratch. A wheel was built
(`python -m build --wheel`) and installed into a SECOND, separate fresh venv outside the repository
entirely; from there, the canonical CLI run reproduced `run_id=r3chain-run-93d41133daa11d1a` and
`bundle_scientific_sha256=ee76b2a626f57fd4825c554ac55e57e81e567f86c7bf4acd771cb23a4389f3c8` exactly
(the same values tabulated above); the generated-candidates and joint-optimization CLI runs
reproduced their own previously-observed `run_id`s exactly; and a direct Python-level exercise of
the persistent MCP registry (construct a registry against a fresh temp root, run the canonical
workflow, close it, construct a NEW registry instance against the same root, confirm zero
rehydration warnings, confirm the summary's `bundle_scientific_sha256` matches, paginate the full
`workflow_result.json` via `geo_get_artifact` to `next_offset: null` and reconstruct it byte-for-
byte, and confirm a repeat `geo_run_workflow` call reports `reused_existing_run: true`) all
succeeded. This proves every MECHANISM behind the Desktop replay works correctly. It does not
replace the replay itself — Claude Desktop is a genuinely different client, process-lifecycle, and
tool-calling loop than this session's own direct Python calls, and this project's own rules require
this specific step to be human-performed, not automated around.
